# jump-cd 设计说明

> 一个只干一件事的小工具。三百行代码的体量，这份文档也就该这么长。

## 1. 目标

**记住你去过哪些目录，让你用几个字跳回去。**

```bash
jcd ~/parent-dir/child-dir/popular   # 第一次：用路径进去，顺便记住
jcd popular                          # 之后：用名字跳回去
```

判据只有一条：上面两行在真实的 shell 里能跑通。其余一切都是可选的。

## 2. 非目标

不做模糊匹配、不做导入导出、不做交互式选择器、不做书签、不做统计面板、不做配置文件。
这些在「以后可能加」的清单上，而不是「现在该有」。

## 3. 架构

```
shell hook（内建 printf + 追加重定向，零进程）
  └─► journal     追加日志：每次 cd 一行
        └─► fold      敲 jcd 时折进历史
              └─► store     dirs.json（文件锁 + 原子改名）
                    └─► jump      匹配 → 排序 → 决策
                          └─► cli       把目标路径打到 stdout，shell 负责 cd
```

两条铁律：

- **子进程改不了父 shell 的 cwd。** 所以二进制只打印路径，`cd` 由 shell 函数执行。
- **热路径不启动进程。** hook 只用 shell 内建追加一行日志 —— 实测 Windows 上启动
  一个二进制的底线是 15.9ms，每个提示符都付这笔钱是无法接受的。

## 4. 数据

```json
{
  "version": 1,
  "dirs": {
    "/home/u/code/jump-cd": { "visits": 3, "last": 1700000000, "first": 1699000000 }
  }
}
```

一个文件，带缩进，你可以直接用眼睛看、用手改。

**为什么不是 SQLite**：那要背 10MB 静态库和十几毫秒的包级初始化，换来事务与并发安全。
对一个「记住我去过哪里」的小工具不划算 —— 换掉之后二进制从 12MB 降到 3.8MB。

**并发怎么保证**：读不需要锁（写是「写临时文件 + 原子改名」，读者永远看不到半个文件）；
写走「空文件锁 + O_EXCL 抢占」，抢不到就等，持有者崩溃留下的陈旧锁会被接管。
两个终端同时敲 `jcd` 也不会互相覆盖。

## 5. 匹配

对每个关键词求它在路径各段上的最强命中档位，多个关键词取**最弱的一环**。

| 情形 | 分 |
|---|---|
| 末段完全相等 | 100 |
| 末段前缀 | 80 |
| 末段子串 | 60 |
| 中间段完全相等 | 50 |
| 中间段前缀 | 40 |
| 中间段子串 | 20 |

## 6. 排序

```
score = 匹配质量 x （1 + 去过几次） x 多久没去
```

- 「去过几次」封顶 20，否则一个天天去的目录会永远压住新目标
- 「多久没去」按半衰期指数衰减，默认 7 天（`JCD_HALF_LIFE_DAYS`）

三项相乘而不是相加：任何一项为零，这条候选就不该存在。

## 7. 决策

前两名分数比值低于阈值（默认 1.25，`JCD_AMBIGUOUS_TAU`）时**不猜**，
把候选列到 stderr 上让用户再说清楚一点。

跳错了比让你多打两个字贵得多。

## 8. Shell 集成

`jcd init <shell>` 输出脚本，脚本里的日志路径在渲染时被替换进去。

| Shell | 记录方式 | 状态 |
|---|---|---|
| bash | `PROMPT_COMMAND` + 内建 printf | **e2e 覆盖** |
| zsh | `add-zsh-hook chpwd` + 内建 printf | 脚本就位，未在真机验证 |
| fish | `--on-variable PWD` | 脚本就位，未在真机验证 |
| PowerShell | 包装 `prompt` + `Add-Content` | 脚本就位，未在真机验证 |
| nushell | `hooks.env_change.PWD` | 脚本就位，未在真机验证 |

函数名与二进制同名（都叫 `jcd`），函数内部用 `command jcd` 调用二进制，
所以它必须在 PATH 上。想要更短的名字加一条 `alias j=jcd` 就行。

注意：bash 的 `PROMPT_COMMAND` 只在**交互式** shell 里执行；同一行里连做多次 `cd`
只会记录最后一次。这与 `cd` 的直觉一致。

## 9. 直接路径优先

`jcd <路径>` 只要那个路径真实存在就直接进去，**不查历史**，也**不碰数据库**。

代价是「当前目录下正好有个同名目录」时它会压过历史记录。这与 `cd` 的直觉一致，
是刻意的取舍。

## 10. 零依赖

只准用标准库，`go.mod` 里没有 `require`。

这意味着：`go build` 不需要联网；二进制里没有别人的代码。代价是没有 CLI 框架
（`flag` 够用）、没有 TOML（改用环境变量）、没有现成的匹配库（自己那六档就够）。
